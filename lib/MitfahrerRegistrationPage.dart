import 'package:flutter/material.dart';

class MitfahrerRegistrationPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {

    return Scaffold(
      appBar: AppBar(
        title: Text('Mitfahrer Registration'),
      ),
      body: Center(
        child: Text('Ovo je Mitfahrer Registration Page'),
      ),
    );
  }
}